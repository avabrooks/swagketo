package com.nighthawk.csa.hacks;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestParam;

/*
@Controller
public class GradeCalcCTRL {
    @GetMapping("services/gradecalc")
    public String TableCTRL(@RequestParam(name = "current", required = false, defaultValue = "4") int current,
                            @RequestParam(name = "desired", required = false, defaultValue = "45") int desired,
                            @RequestParam(name = "percent", required = false, defaultValue = "45") int percent)

                return "/services/gradecalc";
}

*/
